spring:
  application:
    name: projectf
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver